name: ez_permission
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  auto_size_text: ^3.0.0
  # check permission
  permission_handler: ^11.1.0

  

dev_dependencies:
  flutter_test:
    sdk: flutter
