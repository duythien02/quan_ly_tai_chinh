import 'package:flutter/material.dart';

/// Defines the color palette for the App UI.
abstract class AppColors {
  /// primaryColor
  static const Color primaryColor = Color.fromARGB(255, 247, 129, 86);

  /// canvasColor
  static const Color dividerColor = Color(0xffDEE3ED);

  /// primaryColorLight
  static const Color primaryColorLight = Color(0xFFFFF3E0);

  /// hoverColor
  static const Color hoverColor = Color(0xFFFFE0B2);

  /// backgroundColor
  static const Color white = Color(0xffffffff);

  /// errorColor
  static const Color errorColor = Color(0xffC33F38);

  /// hintColor
  static const Color hintColor = Color(0xff707A89);

  /// focusColor
  static const Color success = Color(0xff67AC5B);

  /// primaryColorDark
  static const Color black = Color(0xff000000);

  /// primaryColorDark
  static const Color text = Colors.black;

  /// scaffoldBackgroundColor
  static const Color lightGrey = Color(0xffAEB9CB);

  /// highlightColor
  static const Color highlightColor = Color(0xff008dfa);

  /// toggleableActiveColor
  static const Color blue = Color(0xff344ef0);

  /// selectedRowColor
  static const Color lightBlue = Color(0xFFFF8A65);

  /// bottomAppBarColor
  static const Color shadowColor = Color.fromRGBO(0, 0, 0, 0.1);

  /// unselectedWidgetColor
  static const Color unselectedWidgetColor = Color(0xff858080);

  /// unselectedWidgetColor
  static const Color transparent = Colors.transparent;

  static const Color link = Color(0xff2090fe);
}
