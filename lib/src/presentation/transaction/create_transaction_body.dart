import 'package:flutter/material.dart';

class CreateTransactionBody extends StatelessWidget {
  const CreateTransactionBody({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) {
    return const Center(
      child: Text('Bạn sẽ tạo giao dịch ở đây'),
    );
  }
}
